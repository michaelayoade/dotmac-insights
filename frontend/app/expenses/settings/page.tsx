"use client";

export default function ExpensesSettingsPage() {
  return (
    <div className="space-y-4">
      <div>
        <h2 className="text-xl font-semibold">Expenses Settings</h2>
        <p className="text-sm text-gray-500">Configure categories, policies, and workflows (coming soon).</p>
      </div>
      <div className="rounded-lg border border-dashed border-gray-300 bg-white p-6 text-sm text-gray-600">
        Settings UI placeholder. Hook this to categories, policies, and approval workflow setup.
      </div>
    </div>
  );
}
